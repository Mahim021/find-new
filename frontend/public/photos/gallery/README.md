Her real photos live here locally (`photo-001.jpg` … `photo-068.jpg`,
generated by `scripts/import-photos.mjs`) but are intentionally excluded
from git — this repo is public, and they're private.

To restore them: put her source photos in `sam/` (repo root) and run
`node scripts/import-photos.mjs` from `frontend/`.
